# Backlog

This file is the canonical short-term work tracker for AgentLogViewer.

## Rules

- Keep the backlog focused on product-defining work.
- Do not create bootstrap-only items once the scaffold exists.
- Every item must have a falsifiable `Done When`.
- Prefer a small number of meaningful items over many bookkeeping items.

Status values:
- `todo`
- `in_progress`
- `blocked`
- `done`

Owner zones:
- `domain`
- `application`
- `client-web`
- `board-view`
- `tooling`

## Active Backlog

| ID | Status | Owner Zone | Item | Write Scope | Depends On | Done When | Validation | BOSS Signoff | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ALV-001 | done | domain | Lock the normalized project, backlog, and log contracts for cross-project viewing | `docs/spec.md`, `docs/architecture.md`, `server/index.ts`, `src/shared/types.ts` | None | The docs and implementation define project discovery under configurable roots, backlog column normalization, and the common log fields the UI depends on clearly enough that implementation can proceed without inventing semantics. | Spec review against real project inputs plus implementation review. | yes | Implemented with configurable roots, normalized backlog rows, and normalized log entries. |
| ALV-002 | done | application | Define dashboard and drill-in session behavior | `docs/spec.md`, `docs/architecture.md`, `docs/ui-spec.md`, `src/App.tsx` | ALV-001 | The docs and app define how the app moves between portfolio dashboard, project detail, backlog view, log view, and framework file view, including the default root behavior and configured-root support. | Cross-doc review plus local app build. | no | Implemented with project selection, polling refresh, drill-in, and in-app framework rendering. |
| ALV-003 | done | client-web | Lock the first tile-based dashboard and human-readable log presentation | `docs/ui-spec.md`, `src/App.tsx`, `src/styles.css` | ALV-001, ALV-002 | The UI defines the information shown on project tiles, attention signals, project detail layouts, and readable log presentation with stable filters and groupings. | UX review plus local app build. | yes | Implemented, with stable drill-in, readable logs, instruction-aware backlog rows, and current layout refinements landed. |
| ALV-004 | done | tooling | Define representative real project inputs for parser and dashboard development | `docs/sdlc.md`, `docs/architecture.md`, `server/index.ts`, `scripts/validate-live-refresh.mjs` | ALV-001 | The docs and implementation name existing projects under `C:\work` as representative input sources and use those real backlog/log shapes during development. | Doc review with named project references plus parser checks against local repos. | no | Verified with `npm run validate:live-refresh`, which discovered and parsed real projects including `AgentOpsWorkbench`, `AgentLogViewer`, and `MultiTacToe`. |
| ALV-005 | done | infrastructure | Replace interval polling with filesystem-backed change detection for tracked project files | `docs/spec.md`, `docs/architecture.md`, `server/index.ts`, `scripts/validate-live-refresh.mjs` | ALV-001, ALV-002, ALV-004 | The server watches tracked project backlog, docs, and log directories on Windows and emits refresh signals when relevant files change, so the app no longer depends on a fixed 60-second polling interval for normal updates. | Local verification against multiple real projects with edits to backlog and log files plus implementation review. | yes | Signed off after build plus live-refresh validation against real `C:\work` projects. |
| ALV-006 | in_progress | application | Introduce watcher-driven client refresh with safe fallback and invalidation rules | `docs/spec.md`, `docs/architecture.md`, `docs/ui-spec.md`, `src/App.tsx`, `src/api.ts`, `scripts/validate-live-refresh.mjs` | ALV-005 | The client consumes watcher-driven change notifications, updates affected project data in place, preserves current selection and filters, and falls back to manual or low-frequency refresh when the watcher becomes unavailable. | Local app build plus interaction checks that current mode, selected project, filters, framework tab, and scroll context are preserved across watcher updates. | yes | Build passes and the fallback contract is validated through watcher-status SSE payloads. Remaining gap is browser-level interaction evidence for preserved selection, filters, framework tab, and scroll context. |
| ALV-007 | done | client-web | Graphical backlog board view with zone swimlanes, state badges, and dependency chips | `docs/ui-spec.md`, `src/App.tsx`, `src/styles.css` | ALV-001, ALV-002, ALV-003 | Full-screen board triggered from project tile; one swimlane per zone; cards with ID, title, status badge, dependency chips, notes; auto-scrolls to first active item; done items subdued. | Local app build; verify swimlane grouping, status colors, dep chips, auto-scroll, full-screen toggle. | no | Implemented. Follow-on: global ordering (ALV-008), connectors (ALV-009/010), drag-scroll (ALV-011), click highlight (ALV-012), validation overlay (ALV-013). |
| ALV-008 | done | board-view | Board: global cross-zone card ordering with horizontal gaps | `docs/board-spec.md`, `src/App.tsx`, `src/styles.css` | ALV-007 | Every card is positioned to the right of all its dependencies regardless of zone. Tracks contain horizontal gaps where space is reserved for cross-zone deps. The board reads as a globally consistent left-to-right dependency timeline. | Local board review with a multi-dependency, multi-zone project; verify no card appears left of any of its dependencies across all zones; verify gaps appear correctly. | no | Implemented via `computeBoardLayout` with iterative column assignment and gap spacers. |
| ALV-009 | done | board-view | Board: orthogonal cross-zone connector lines with arrowheads | `docs/board-spec.md`, `src/App.tsx`, `src/styles.css` | ALV-008 | Right-angle connector lines with arrowheads link cards to dependencies across all zones. Lines exit top edge for same/higher tracks, bottom edge for lower tracks. Route: vertical to gutter, horizontal to target column, vertical to target card. Full-board SVG overlay (not per-swimlane). | Local board review; verify connectors appear for both same-zone and cross-zone deps; verify routing directions, arrowheads, and done/active color coding per board-spec.md. | no | Implemented via `BoardSvgOverlay` with imperative DOM, arrowhead markers, and directional routing. |
| ALV-010 | done | board-view | Board: hub routing and non-overlapping connector layout | `docs/board-spec.md`, `src/App.tsx`, `src/styles.css` | ALV-009 | Items that are dependencies for many others use a shared horizontal rail above/below the track with branches to each dependent. Multiple connectors sharing a gutter are offset vertically so lines never overlap and each is individually traceable. | Local board review with a hub item (3+ dependents); verify shared rail routing and no line overlap in gutters. | no | Hub threshold is 3+ dependents in same direction. Gutter offset is 8px per line. |
| ALV-011 | done | board-view | Board: drag-to-scroll on board surface | `docs/board-spec.md`, `src/App.tsx`, `src/styles.css` | ALV-007 | Click-and-hold on any non-card area of the board, then drag, pans in both axes. Cursor shows `grab` on hover, `grabbing` while dragging. Does not interfere with card click/select. | Local board interaction; verify drag-scroll works on empty board space; verify card clicks still work normally. | no | Uses pointer events with capture for reliable cross-element tracking. |
| ALV-012 | done | board-view | Board: click-to-highlight dependencies and dependents with completion color coding | `docs/board-spec.md`, `src/App.tsx`, `src/styles.css` | ALV-007 | Clicking a card highlights immediate dependencies (distinct color for done vs active) and immediate dependents per board-spec.md highlight table. Unrelated cards dim to 25% opacity. Click again or elsewhere clears. | Local board interaction with a multi-level dependency project. | no | Implemented in Swimlane via getHighlight with CardHighlight CSS classes. |
| ALV-013 | done | board-view | Board: validation status indicators and validation overlay on done cards | `docs/board-spec.md`, `src/App.tsx`, `src/styles.css` | ALV-007 | Done cards show correct validation state per board-spec.md states table. Validate/Validated affordance present. Clicking opens portal overlay with full validation instructions and fingerprint stale warning. Stale fingerprint detection works. | Local board review with done items; overlay behavior matches board-spec.md overlay rules. | no | Overlay uses createPortal to document.body; localStorage-backed fingerprints. |
| ALV-014 | done | board-view | Board: fade out non-participating cards on selection | `docs/board-spec.md`, `src/App.tsx`, `src/styles.css` | ALV-012 | Selecting a card causes all non-participating cards to animate from opacity 1 to 0 over ~250ms, then set `display: none` to remove from flow. Deselecting reverses: `display` restored, opacity animates 0 to 1. | Local board interaction on hexbuckle2; select a card with cross-zone deps, verify non-participating cards fade and disappear; deselect, verify they return. | no | |
| ALV-016 | done | board-view | Board: collapse empty swimlanes and compact participating cards | `docs/board-spec.md`, `src/App.tsx`, `src/styles.css` | ALV-014 | Swimlanes containing zero participating items collapse (label and track hidden). Participating cards in remaining swimlanes reflow compactly as non-participating siblings are removed from flow. On deselection, collapsed swimlanes re-expand and all cards return to original positions. | Local board interaction; select a card whose deps span 2 of 5 zones; verify the other 3 zone swimlanes collapse; verify remaining cards pack together; verify full restore on deselect. | no | |
