# Docs Index

Use this docs layout when the repo needs stronger separation between product specs and project execution guidance.

## Layout

- `docs/specs/` holds durable product and architecture specs
- `docs/project/` holds backlog, SDLC, validation, workstreams, decisions, and milestones
- `agents/` holds planner/coder/evaluator loop guidance
